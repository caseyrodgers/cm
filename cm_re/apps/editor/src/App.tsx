// Top-level shell: SolutionList -> StepEditor, plus a "publish module"
// action that hits the backend's module-publish endpoint once a subject's
// solutions are ready to bump.
// TODO: routing between SolutionList and the active StepEditor.
