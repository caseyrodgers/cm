// Thin fetch wrapper for the backend API. Unlike tutor's client.ts, this
// one can assume connectivity — editor is online-only by design.
// TODO: listSolutions(), getSolution(id), saveSolution(doc), publishModule(subjectId)
