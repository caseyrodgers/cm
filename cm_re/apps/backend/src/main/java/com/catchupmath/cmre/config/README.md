# config/

Spring `@Configuration` classes — datasource, CORS (tutor/editor are
separate origins from the backend in local dev), security/auth.
Empty for now; this is a placeholder so the package survives in git
before real config classes exist.
