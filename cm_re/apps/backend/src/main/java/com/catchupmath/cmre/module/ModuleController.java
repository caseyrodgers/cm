package com.catchupmath.cmre.module;

// REST endpoints consumed by apps/tutor (manifest check + module fetch)
// and apps/editor (trigger publish).
// TODO: GET /api/modules/:subjectId (manifest), POST /api/modules/:subjectId/publish
public class ModuleController {
}
