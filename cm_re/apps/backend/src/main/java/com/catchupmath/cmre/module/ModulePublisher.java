package com.catchupmath.cmre.module;

// The "publish a module bundle" build step from NEW_DIRECTION.org's
// content-module design: given a subjectId, gather that subject's
// SolutionDocuments via SolutionService, hash the content, bump the
// manifest version, write the bundle wherever apps/tutor fetches it from.
// TODO: publish(String subjectId) -> ModuleManifest
public class ModulePublisher {
}
