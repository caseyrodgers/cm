# test/

Empty for now — placeholder so the package survives in git. First
tests should cover SolutionService (version bumping on save) and
ModulePublisher (manifest hash changes when solution content changes).
